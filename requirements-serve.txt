# Serving-only dependencies for the deployed Micro-RAG container (PINNED for a
# reproducible build). The live app only does retrieval + grounding + serving, so it
# needs NONE of the dataset-building deps (pandas, lxml, openpyxl, bs4, requests, pytest).
# Keeping the image lean makes the free-tier build fast and reliable.
# The full dev/pipeline deps live in requirements.txt / requirements.lock.
fastembed==0.8.0
rank-bm25==0.2.2
numpy==2.5.1
pydantic==2.13.4
python-dotenv==1.2.2
fastapi==0.140.1
uvicorn[standard]==0.51.0
groq==1.6.0
openai==2.53.0
# heavy transitive deps of fastembed, pinned so the ONNX embedding stack is deterministic
onnxruntime==1.28.0
tokenizers==0.23.1
